[formatter.nix]
command = "nixfmt"
includes = ["*.nix"]

[formatter.go]
command = "golangci-lint"
options = ["fmt"]
includes = ["*.go"]

[formatter.prettier]
command = "prettier"
options = ["--write"]
includes = [
  "*.css",
  "*.js",
]
excludes = ["*.min.js"]
